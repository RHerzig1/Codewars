// 

function numObj(arr){
  return arr.map(elem => { return { [elem]: String.fromCharCode(elem) }}
)}


// An array.
// An object containing strings.
// Use the map() method to iterate through the array.
// Create a object with the elem key and then CharCode value.

// https://www.codewars.com/kata/57ced2c1c6fdc22123000316