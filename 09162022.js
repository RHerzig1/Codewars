// Sum of all elements in array

function sum(numbers) {
  return numbers.reduce((a, b) => a + b, 0)
};

// The parameters are an array of integers.
// Return the sum of all the elements or, if the array is empty, 0.
// Use the reduce method to add the elements.
// Return the result.