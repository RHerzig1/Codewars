// Compare all elements of array against testing function

function all(arr, fun) {
  return arr.every(fun)
}

// The parameters are an array of integers and a function.
// Return true or false, whether every element passes the implimented testing function.
// Use the every() method on arr. Pass fun as the callback function.
// Return the result.