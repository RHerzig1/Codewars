// Find the maximum achievable number.

function theMaximumAchievableX(num: number, t: number): number {
    return num + (t * 2);
};

// https://leetcode.com/problems/find-the-maximum-achievable-number/
// Seriously, why is this even a leetcode question???