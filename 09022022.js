// Concatenate strings without the + operator or concat() and join() methods. 

function joinStrings(string1, string2){
  return `${string1} ${string2}`
}

// There are two parameters, containing either a string or a number.
// Return the concatenation of string1 and string2, seperated by a space.
// ('hello', 'world') -> 'hello world'
// Concatenate the strings using template literal.
// Return the result.